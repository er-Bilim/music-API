# Music API

### Backend (API)

Start the server

```bash
npm run dev
```

## tsx dependency

Использую `tsx` вместо `nodemon`

`tsx` работает быстрее + поддерживает модули. У меня в package.json поле type установлено 'module'. C `nodemon` она не работает без доп конфигураций